import { v4 } from "uuid";

export const joinRoom = () => {
    const newid = v4();
    // socket.join(newid , )
    // Socket.emit("joinRoom", router.query.room)
    // router.push(`/chat/${router.query.room}`)
  }